import React from 'react';
import FormRegister from './FormRegister';

const FrontPage = () => (
  <div className="main-container">
    <div>
      <p>
        Just some random text where most of the design would go to and the about
        section would go?
      </p>
    </div>
    <FormRegister />
  </div>
);

export default FrontPage;
